# D 同学答辩报告 — 检验鉴定模块

> **模块职责**：版权图片的自动侵权鉴定  
> **一句话总结**：用户上传嫌疑图片 → 系统自动判断是否盗版、是否有篡改

---

## 一、我做了什么

我负责的是整个系统最核心的"检验鉴定模块"。一句话概括——**从一张嫌疑图片里，用 ELA 看它有没有被动手脚，用水印看它是不是偷的我们的图，然后综合两条证据给出鉴定结论。**

具体写了 3 个 Python 文件，共 540 行代码：

| 文件 | 行数 | 功能 |
|------|------|------|
| `ela.py` | 184 | ELA 篡改检测 + 热力图生成 |
| `watermark_check.py` | 143 | 水印提取 + 三种策略比对 |
| `validator.py` | 213 | 综合鉴定器：串联流程 + 证据矩阵判定 |

---

## 二、ELA 篡改检测

### 2.1 原理（一句话版）

**ELA 不需要原图，只靠嫌疑图片自己就能检测篡改。** 把嫌疑图用统一质量 Q=90 重新保存一次 JPEG，比较"重压前"和"重压后"的像素差异。

- 背景区域：可能被网上转存压缩过很多次，再压一次变化很小 → 差异低（蓝色）
- 贴入的人像/素材：来自其他图，压缩次数完全不同 → 变化很大 → 差异高（红色）

### 2.2 实现流程

```
读取图片 → JPEG Q=90 重保存 → 逐像素做差 → 差值×15放大 → 异常像素占比>5%即报警 → 生成蓝红热力图
```

### 2.3 技术要点

- **纯 numpy + PIL 实现**，不依赖 matplotlib
- **手写 5 段 jet 伪彩色映射**：蓝→青→绿→黄→红→白
- 4 个可调参数：压缩质量、放大倍数、异常阈值、比例阈值
- 异常安全：finally 块自动清理临时文件

### 2.4 实际效果

测试用一张 200×200 纯色背景 + 中间红色方块的模拟篡改图：
- 系统正确检测到异常像素占比 **9.54%**（阈值 5%）
- 生成热力图中间区域呈**红/白色**（高度可疑），背景呈蓝色（正常）

---

## 三、水印校验

### 3.1 水印提取（底层原理）

系统发布作品时，用 DCT 频域盲水印把版权信息嵌入图片亮度通道。提取时不需要原图：

- 每个 8×8 DCT 块的 (2,3)(3,2) 中频系数对编码 1 bit
- (3,1) 重复编码，提取时多数表决（3 取 2）
- 滑动窗口搜索 16bit 同步头
- 多副本投票去重，抵御误匹配

### 3.2 三级比对策略

| 优先级 | 方式 | 条件 |
|--------|------|------|
| 1 | **精确匹配** | 提取 == 预期（完全一致） |
| 2 | **子串匹配** | 预期 ⊆ 提取 或 提取 ⊆ 预期 |
| 3 | **模糊匹配** | Levenshtein 编辑距离相似度 ≥ 70% |

编辑距离算法用了滚动数组优化，O(n) 空间复杂度。

---

## 四、综合鉴定器

### 4.1 鉴定流水线

```
SHA-256 文件哈希 → ELA 篡改检测 → 水印提取 → 水印比对 → 综合判定
```

### 4.2 六级证据判定矩阵

| 水印提取 | 水印匹配 | ELA异常 | 结论 |
|----------|---------|---------|------|
| ✅ | ✅ | ✅ | ⚠ **支持侵权认定**（双证据） |
| ✅ | ✅ | ❌ | ⚠ **支持侵权认定**（水印证据） |
| ✅ | ❌ | ✅ | 🔶 **存在嫌疑**，需人工复核 |
| ❌ | — | ✅ | 🔶 **存在嫌疑**，需人工复核 |
| ✅ | ❌ | ❌ | ❓ 证据不足 |
| ❌ | — | ❌ | ❓ 证据不足 |

### 4.3 输出内容

- 每一步的**时间戳审计日志**（`sha256 → ela → watermark_extract → watermark_match → complete`）
- **ELA 热力图 PNG** 文件路径
- **结论 JSON**：含文件哈希、ELA 结果、水印内容、比对方式、最终判定

---

## 五、前后端对接

我同时修改了 Flask 路由和前端 JavaScript，把鉴定结果完整展示在页面上：

- 新增 `/forensics/api/forensics/ela-image/` 路由，直接返回热力图 PNG
- 前端渲染：判定级别徽章（红/橙/灰）、ELA 异常详情 + 热力图、水印提取 + 比对结果、最终结论文字

---

## 六、答辩关键数据

| 指标 | 数值 |
|------|------|
| 代码量 | 540 行 Python（3 文件） |
| 核心算法 | ELA 差分分析 + Levenshtein 编辑距离 |
| 热力图实现 | 纯 numpy 手写，零额外依赖 |
| 水印匹配策略 | 3 级（精确 → 子串 → 模糊，70%阈值） |
| 鉴定结论级别 | 4 级（supported / suspicious / inconclusive / error） |
| 证据判定组合 | 6 种场景全覆盖 |

---

*报告时间：2026-05-30*